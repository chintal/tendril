
language: python
python: 2.7
env:
  - TOX_ENV=py27
  - TOX_ENV=docs
  - TOX_ENV=style
install:
  - pip install -U pip wheel
  - pip install tox
cache:
  directories:
    - $HOME/.cache/pip
script:
  - tox -v -e $TOX_ENV
