%{#
    Copyright (c) 2015 Chintalagiri Shashank
    Released under the MIT license
%#}
\documentclass[subject=centered, fromalign=right, foldmarks=false, fontsize=10pt, DIV=13]{scrlttr2}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{graphicx}
\usepackage{tfrupee}
\pdfmapfile{=tfrupee.map}

\makeatletter
\@setplength{toaddrvpos}{3.5cm}
\@setplength{refvpos}{6.5cm}
\makeatother

\begin{document}
\setkomavar{signature}{For %{{stage.company%}}\\%{{stage.signatory%}}}
\setkomavar{location}{\sffamily\scriptsize \small \raggedleft \textbf{\tiny INTERNAL REFERENCE} \\ \texttt{%{{stage.sno%}}}}

\setkomavar{subject}{{Note on Valuation}\\\small\textit{For import of contents of %{{stage.given_data.supplier_name%}} Invoice No. %{{stage.given_data.invoice_no%}},\\shipped via %{{stage.given_data.shipper%}} AWB %{{stage.given_data.shipper_awb_no%}}}}

\begin{letter}{The Deputy Commissioner of Customs\\
		New Courier Terminal,\\
		I.G.I Airport,\\
		New Delhi, India}
		
		
\opening{Dear Sir/Madam,}
% \pagestyle{fancy}
\small{
  The valuation of the items imported is as per {\tt Rule 4(1) of the Customs Valuation (Determination of Value of Imported Goods) Rules, 2007}, i.e., the price as paid. The Invoice (in %{{stage.currency%}}) numbered %{{stage.inv_no%}} \ issued by %{{stage.given_data.supplier_name%}} to us, i.e., %{{stage.company%}} is attached as proof of the same. %{ if stage.is_wire == True %}Also attached is a copy of the Debit Advice for Import Advance Remittance issued to us by our Bank (%{{stage.given_data.bank%}}), Bill Reference No. %{{stage.given_data.bank_ref%}}, for the Wire Transfer via SWIFT executed as pre-payment for the purchase. %{ endif %}
  \vspace{0.8em}
  
  Valuation is done on the basis of the total invoice amount of %{{stage.inv_total%}}, as detailed in the %{{stage.given_data.supplier_name%}} Invoice. %{{stage.note1%}} 
  %{if stage.include_note2==True%} Additional components added to the valuation for the purposes of customs assessment are : 
  \begin{enumerate} 
    %{for note in stage.note2%} \item %{{note|escape_latex%}} %{endfor%}
  \end{enumerate}
  %{endif%}
  
  An exchange rate of \rupee~%{{stage.exchrate%}} / %{{stage.currency%}}1 is applied vide {\tt Notification No.%{{stage.exchnotif%}}}, dated %{{stage.exchnotifdt%}}. 
  \vspace{0.8em}
  
  %{{stage.given_data.assessaddendum or ''%}}
  %{if stage.is_wire == True %} Additional charges borne by us in the form of Bank Fee, paid to %{{stage.given_data.bank%}}, India, for the wire transfer, and Government of India Taxes and Cesses on this financial transaction between us, i.e., %{{stage.company%}} and our Indian banker, i.e., %{{stage.given_data.bank%}}, have not been included in the valuation. %{ endif %}
  \vspace{0.8em}
  
  The total valuation of the contents of the shipments as calculated comes out at %{{stage.assessable_total_sc%}}, or %{{stage.assessable_total_nc%}}, which is used in the Customs Declaration Form. 
 }
\small 
\vspace{1em}
\closing{Sincerely,\vspace{3em}}

\end{letter}

\end{document}
